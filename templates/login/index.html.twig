{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    {{parent()}}


    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-6 bg-white rounded my-3 p-5">
                <h1>Inicia Sesión</h1>
                <form action="{{ path('login') }}" method="post">
                    <div class="form-group">
                    <label for="username">Email:</label>
                    <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control"/>
                    </div>
                    <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="_password" class="form-control"/>
                    </div>
                    {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"/> #}
                    <div class="form-group align align-items-center align-content-center fill">
                    <button type="submit" class="flex-fill btn-primary btn align-center align-content-center">login</button>
                    <button type="button" class="flex-fill btn-outline-primary btn align-self-end" href="{{ path('registro') }}">No tienes cuenta? <span class="sr-only">(current)</span></button>
                    </div>
                    {% if error %}

                    <div>ERROR:{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
            </div>
        </div>
    </div>

    </form>
{% endblock %}
